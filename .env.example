# Optional: for contact form and subscribe on GitHub Pages (static host).
# Create forms at https://formspree.io and paste the form endpoint URLs.
# Leave unset when running locally with `npm run dev` (API routes handle forms).
# NEXT_PUBLIC_FORMSPREE_CONTACT=https://formspree.io/f/xxxxxxxx
# NEXT_PUBLIC_FORMSPREE_SUBSCRIBE=https://formspree.io/f/xxxxxxxx

# Medusa backend (storefront calls Store API with publishable key).
# Backend URL when Medusa is running (e.g. local or deployed).
# NEXT_PUBLIC_MEDUSA_BACKEND_URL=http://localhost:9000
# Publishable API key from Medusa admin: Settings → Publishable API Keys.
# NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=pk_xxxxxxxx
# Region ID (e.g. Canadian region) so product prices are in that region's currency (CAD). Get from Admin → Settings → Regions, or: curl -H "x-publishable-api-key: YOUR_KEY" "http://localhost:9000/store/regions"
# NEXT_PUBLIC_MEDUSA_REGION_ID=reg_xxxxxxxx

# Stripe (checkout): must be from the same Stripe account as store_backend STRIPE_API_KEY.
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxx
